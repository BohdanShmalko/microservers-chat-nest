export enum EMessageTypes {
  Member = 'member',
  User = 'user',
}

export enum EFileTypes {
  Video = 'video',
  Audio = 'audio',
  Image = 'image',
  Any = 'any',
}

export enum EMessageStatus {
  Dispatch = 'dispatch',
  Read = 'read',
  Send = 'send',
}
